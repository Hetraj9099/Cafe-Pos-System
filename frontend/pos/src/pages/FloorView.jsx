import PlaceholderPanel from '../components/PlaceholderPanel';

const FloorView = () => {
  return (
    <PlaceholderPanel
      title="Floor View"
      description="Placeholder layout for tables, seating zones, and service assignment."
    />
  );
};

export default FloorView;
