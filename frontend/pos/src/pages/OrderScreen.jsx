import PlaceholderPanel from '../components/PlaceholderPanel';

const OrderScreen = () => {
  return (
    <PlaceholderPanel
      title="Order Screen"
      description="Placeholder workspace for menu browsing, order composition, and ticket actions."
    />
  );
};

export default OrderScreen;
