import PlaceholderPanel from '../components/PlaceholderPanel';

const OrderPage = () => {
  return (
    <PlaceholderPanel
      title="Order Page"
      description="Placeholder for guest menu browsing, item selection, and QR-linked ordering."
    />
  );
};

export default OrderPage;
